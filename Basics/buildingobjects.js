let episode = {
    title: 'The Witcher',
    duration: 60,
    hasBeenWatched: true
};

let episodeTitle = episode.title;
let episodeDuration = episode.duration;
let episodeHasBeenWatched = episode.hasBeenWatched;


console.log (episode.title, episode.duration, episode.hasBeenWatched);
